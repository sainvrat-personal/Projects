# Local development profile

# Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/orders_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.springframework=DEBUG
logging.level.com.example.orderservice=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Email settings - disabled for local development unless explicitly provided
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:sainvrat@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:qjtxzvcsclhioigp}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Enable email sending for local development if credentials are provided
email.sending.enabled=${EMAIL_ENABLED:true}

# External service URLs
mock.payment.url=http://localhost:8080/mock-payment/refund
